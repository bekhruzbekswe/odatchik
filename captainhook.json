{
  "commit-msg": {
    "enabled": true,
    "actions": {
      "conventional-commit": {
        "action": "\\CaptainHook\\App\\Hook\\Message\\Action\\Regex",
        "options": {
          "regex": "#^(feature|fix|docs|refactor|test|chore)(\\(.+\\))?:\\s.+#",
          "error": "Commit message must follow conventional commits format:\ntype(scope?): subject\n\nAllowed types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert\n\nExamples:\n  feat: add user authentication\n  fix(api): resolve null pointer exception\n  docs: update README with setup instructions"
        },
        "conditions": []
      }
    }
  },
  "pre-commit": {
    "enabled": true,
    "actions": {
      "pint-fix": {
        "action": "bash -c 'git diff --cached --name-only --diff-filter=ACM | grep \"\\.php$\" | xargs -r ./vendor/bin/pint && git diff --cached --name-only --diff-filter=ACM | grep \"\\.php$\" | xargs -r git add'",
        "options": [],
        "conditions": []
      },
      "larastan": {
        "action": "bash -c 'git diff --cached --name-only --diff-filter=ACM | grep \"\\.php$\" | xargs -r ./vendor/bin/phpstan analyse --memory-limit=2G'",
        "options": [],
        "conditions": []
      }
    }
  }
}